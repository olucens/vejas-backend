# Server
PORT=3000
# Allowed frontend origin for CORS (use the deployed frontend URL in prod)
CORS_ORIGIN=http://localhost:4200

# Supabase project URL — used to verify JWTs via the project's JWKS endpoint
SUPABASE_URL=https://ozgvczufhxhctexfexpr.supabase.co
# Optional: legacy HS256 JWT secret (Supabase dashboard → Settings → API).
# If set, it takes precedence over JWKS verification.
# SUPABASE_JWT_SECRET=
